import styled from '@emotion/styled'
import { grid, layout, space } from 'styled-system'
import Box from '../Box'

const Grid = styled(Box)`
  display: grid;
  ${space}
  ${grid}
`

export default Grid
